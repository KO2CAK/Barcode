export default function Footer() {
  return (
    <footer className="mx-auto w-full max-w-6xl px-5 py-10 text-center text-sm text-zinc-500">
      <p>
        Built as a static QR & Barcode Generator. No backend, no account, no
        uploaded data stored on a server.
      </p>
    </footer>
  );
}
